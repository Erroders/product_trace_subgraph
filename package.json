{
  "name": "product_trace_subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ product_trace_subgraph",
    "create-local": "graph create --node http://localhost:8020/ product_trace_subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ product_trace_subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 product_trace_subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.30.4",
    "@graphprotocol/graph-ts": "0.27.0",
    "is-ipfs": "^7.0.3"
  }
}
